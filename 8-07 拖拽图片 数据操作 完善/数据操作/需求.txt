需求：
1. 先把已有的数据渲染在页面中
2. 点击添加，要添加新数据并且添加新结构
3. 全选单选
4. 点击删除，删除当前这个结构并且还要删除对应的数据
5. 批量删除 删除页面中的结构和数据
6. 按年龄排序

步骤：
1. 先把已有的数据渲染在页面中
2. 点击添加按钮
    姓名和年龄不能为空
    tbody中有新添加的数据的结构
        tr td
    data中也要添加上新添加这条是数据
        {
            id
            ...
        }

    判断是否点击过排序按钮
            点过，就要按照排序的方式
            没点过 追加

3. 全选单选
    都要获取到元素

4. 删除
    tbody.removeChild(tr);
    删除数据
        通过id删除

6. 排序
        如果单条是选中的，排序后依然是选中的

        可以给每条数据添加上一个状态
                selected:true/false